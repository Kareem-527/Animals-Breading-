export const EXAM_DURATION_MINUTES = 60;
export const PASSING_SCORE_PERCENTAGE = 60;

// Mapping specific answer keys if needed, though mostly standard A/B/C
export const OPTION_LABELS = ['A', 'B', 'C', 'D'];